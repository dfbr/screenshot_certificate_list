# This workflow has been disabled to avoid running on push. See `.github/workflows/screenshot.yml` for the scheduled/manual workflow.
name: "disabled-run-screenshot_certificate_list"

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: |
          echo "This workflow is intentionally disabled."
          echo "Use .github/workflows/screenshot.yml instead."
